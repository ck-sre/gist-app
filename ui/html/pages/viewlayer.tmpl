{{ define "heading"}}Gist #{{ .TmplGst.ID }}{{end}}
{{ define "start"}}
   {{ with .TmplGst }}
        <div class='gist'>
            <div class='metadata'>
                <strong>{{ .Title }}</strong>
                <span>#{{ .ID }}</span>
            </div>
            <pre>{{ .Content }}</pre>
            <div class='metadata'>
                <span>{{ .CreatedOn.Weekday }}</span>
                <time>{{ .ExpiresOn.AddDate 0 1 0 }}</time>
            </div>
        </div>
    {{ end }}
{{ end }}